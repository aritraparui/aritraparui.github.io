<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>AriX Player - NextGen Music Streaming</title>
    <style>
        :root {
            --primary: #ff4b4b;
            --secondary: #6c5ce7;
        }
        body {
            background: linear-gradient(135deg, #1a1a1a 0%, #2d3436 100%);
            color: white;
            font-family: 'Segoe UI';
        }
        .player-container {
            width: 400px;
            margin: 2rem auto;
            padding: 2rem;
            background: rgba(255,255,255,0.1);
            backdrop-filter: blur(10px);
            border-radius: 20px;
            box-shadow: 0 8px 32px rgba(0,0,0,0.1);
        }
        .neumorphic {
            background: var(--bg);
            box-shadow: 9px 9px 16px rgba(0,0,0,0.5),
                       -9px -9px 16px rgba(255,255,255,0.05);
        }
    </style>
</head>
<body>
    <div class="player-container neumorphic">
        <div id="player"></div>
        <div class="controls">
            <button id="play">▶</button>
            <input type="range" id="volume" min="0" max="100">
            <div id="current-track"></div>
        </div>
    </div>

<script>
const API_KEY = 'AIzaSyBtRey4UY_MKDI2eKWdJhG3xZDPhXvdjpU';

async function loadYTM() {
    const response = await fetch(`https://youtube.googleapis.com/youtube/v3/search?part=snippet&maxResults=50&q=music&key=${API_KEY}`);
    const data = await response.json();
    return data.items;
}

function initPlayer(videoId) {
    return new YT.Player('player', {
        height: '200',
        width: '400',
        videoId: videoId,
        playerVars: {
            'autoplay': 1,
            'controls': 0,
            'disablekb': 1
        }
    });
}

window.onYouTubeIframeAPIReady = async () => {
    const tracks = await loadYTM();
    let currentTrack = 0;
    const player = initPlayer(tracks[currentTrack].id.videoId);
    
    document.getElementById('play').addEventListener('click', () => {
        player.getPlayerState() === 1 ? player.pauseVideo() : player.playVideo();
    });
};
</script>
<script src="https://www.youtube.com/iframe_api"></script>
</body>
</html>
